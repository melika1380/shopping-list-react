import React from 'react';
import styles from "./Header.module.css";

const header = () => {
    return (
        <div>
            <div className={styles.header}>Shopping List</div>
        </div>
    );
};

export default header;